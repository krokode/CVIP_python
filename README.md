# CVIP_python

This repository contains Python projects and assignments for the Computer Vision and Image Processing course. It includes weekly exercises, demonstrations, and submission scripts built with OpenCV, NumPy, and Matplotlib.

## Repository Structure

- `submission.py` - A sunglasses filter demo that detects faces and overlays transparent sunglasses.
- `final_projects.py` - Final project entry point for course deliverables.
- `requirements.txt` - Python dependencies for running the repository scripts.
- `musk.jpg` - Example source image used by the sunglasses filter demo.
- `sunglass.png` - Transparent sunglasses image with an alpha channel used for overlay.
- `week1_python/` through `week6_python/` - Weekly assignment folders containing course exercises, sample data, and submission files.

## Getting Started

1. Create and activate a Python virtual environment:

   ```powershell
   python -m venv .CVIP_py_env
   .\.CVIP_py_env\Scripts\Activate.ps1
   ```

2. Install dependencies:

   ```powershell
   pip install -r requirements.txt
   ```

3. Run the main demo:

   ```powershell
   python .\submission.py
   ```

## Notes

- 

## Recommended Files

- `requirements.txt` - Install OpenCV, NumPy, Matplotlib, and any other required packages.
- `README.md` - This file provides the repository overview and usage instructions.
